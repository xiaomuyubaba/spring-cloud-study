app:
  version: UNIT_TEST

server:
  port: 8001

spring:
  application:
    name: pay-service-test
  jackson:
    date-format: yyyy-MM-d HH:mm:ss
    time-zone: GMT+8

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml